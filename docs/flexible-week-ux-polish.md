# Flexible Week conservative UX polish — 2026-09-06

1. **Placement:** Adjust Week moved out of the week selector into the selected-day eyebrow row. A shared Today render helper uses the existing Eyebrow and text-button for planned, rest, completed, moved-source and active-day summary states. The date strip and week navigation are untouched.
2. **Vertical effect:** actual before/after title coordinates show a 36px upward move in every one of the 24 width/theme/day fixtures. Only the standalone action-row allocation was removed; surrounding margins and title typography were not changed. Grid layout keeps the two labels centered; negative block margins allow the 44px button target without allocating another full-height row. No absolute positioning is used.
3. **Missed root action:** native disabled button when there are no eligible missed candidates, with “No missed workouts to move.” It remains visible with neutral disabled semantics and readable helper text; other root cards are unchanged.
4. **Race fallback:** the downstream empty state remains. Component tests rerender with eligible work appearing at the root and disappearing after navigation; no stale selectable row remains.
5. **Train today instead:** underline removed, semantic quiet accent retained, 44px target preserved. It remains a native keyboard-accessible button using existing focus-visible treatment and opens the same activity sheet. View Workout remains the larger bordered action.
6. **Choose a workout:** only the `pick` step now uses divided, transparent list rows, 68px minimum with 13px vertical padding. Existing title/date typography is unchanged; long names wrap and grow naturally. No icons or chevrons. Missed-session cards and root cards are not compacted.
7. **Reuse:** existing Eyebrow, text-button, choice-row content and sheet scaffold; list spacing follows the existing rest-training/move-workout divider pattern using theme tokens. No new navigation or domain component.
8. **Responsive QA:** 320/390/430px, both workout/rest day fixtures. Assertions cover non-overlapping centered header items, 44px action targets, no page overflow, at least six selectable sessions, selected-row identity/title, rest shortcut behavior and the unchanged date pickers. Before/after screenshots and measured coordinates are in `artifacts/flexible-week-polish/` (248 PNGs plus measurement JSON). At narrow width, long workout names wrap without reducing font size. Current dates are English; the app does not expose localized date-format selection. Grid allows longer left text to wrap rather than overlap.
9. **Themes:** Standard Light/Dark and Premium Light/Dark passed. Disabled missed option remains neutral, without active gold emphasis; enabled text actions use existing semantic theme accents. No scaffold/close/back styling changes.
10. **Tests:** new `FlexibleWeekSheet.test.jsx` has two component cases for disabled/nonactivation, state updates and defensive race fallback; 31 existing Flexible Week domain cases still pass. Runtime scripts passed: `flexible-week-polish-qa.mjs` (24 before + 24 after cases), `flexible-week-qa.mjs` (12 complete move/source/destination/reload/restore flows), `flexible-week-states-qa.mjs` (missed/fewer/carry/skip/persistence/clean-install ZIP), `flexible-week-integration-qa.mjs` (Adjust Today entry, moved start/reload/completion/History/Weekly Review), `workout-resume-qa.mjs`, and `header-alignment-qa.mjs` (12 cases).
11. **Full suite:** 786 tests / 36 files passed.
12. **Production build:** passed; existing large-chunk advisory remains.
13. **Diff check:** `git diff --check` passed, with existing LF/CRLF advisories only.
14. **Files changed in this pass:** `src/App.jsx`, `src/FlexibleWeekSheet.jsx`, `src/calendar.css`; added `src/FlexibleWeekSheet.test.jsx`, `scripts/flexible-week-polish-qa.mjs`, this report. Existing dirty work from prior tasks was preserved. No scheduling engine, domain, block, progression, backup schema, active logging or KG/REPS/RIR edits.
15. **Unverified:** physical iPhone/installed-PWA touch, native safe-area/home-indicator behavior, physical keyboard/assistive-technology behavior. Desktop Chromium screenshot and native-button assertions are not physical-device verification. No new external AI review was requested or performed for this four-change pass; the previous approval refers to the prior implementation.

No push or commit.
